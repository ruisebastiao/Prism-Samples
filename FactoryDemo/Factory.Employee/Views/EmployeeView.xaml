<Controls:MetroContentControl x:Class="Factory.EmployeeModule.Views.EmployeeView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Width="525" BorderThickness="1" TransitionsEnabled="True" >


    <Grid>
        <Border BorderThickness="1" CornerRadius="5" BorderBrush="Gray">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="55*"/>
                </Grid.RowDefinitions>
                <TextBlock Background="{DynamicResource AccentColorBrush}" TextAlignment="Center" TextWrapping="Wrap" Text="{Binding Employee.FirstName}"
                 >

                </TextBlock>

            </Grid>

        </Border>

    </Grid>

</Controls:MetroContentControl>
